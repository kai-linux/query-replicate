Quick and dirty solution to generate images on replicate (portrait format, png)

Just add your API key to the config and run main

python main.py --prompt <your prompt> --n <no. img> --title <file name> --model <model name>
